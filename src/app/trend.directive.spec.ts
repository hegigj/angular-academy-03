import { TrendDirective } from './trend.directive';

describe('TrendDirective', () => {
  it('should create an instance', () => {
    const directive = new TrendDirective();
    expect(directive).toBeTruthy();
  });
});
